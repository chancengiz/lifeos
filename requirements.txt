Django>=5.0,<6.0
psycopg[binary]>=3.1
pgvector>=0.2.5
celery>=5.3
redis>=5.0
django-celery-beat>=2.6
django-environ>=0.11
cryptography>=42.0
PyYAML>=6.0

# LLM access (SPEC 10). Tier routing lives in config/llm_tiers.yaml.
anthropic>=1.0
# Optional second backend, imported lazily. Required only for tiers whose
# backend is `litellm` - today that is TIER_EMBED, since the Anthropic API has
# no embeddings endpoint.
# litellm>=1.40

# dev / test
pytest>=8.0
pytest-django>=4.8
